Note: sometimes when downloading python or a framework/library, it will download with errors. In those cases, you have to install them separetely and re-compile

sudo apt update && sudo apt upgrade -y

# Basic build tools and libraries
sudo apt install -y python3-pip python3-venv git

# Optional: for WANDB and data logging
sudo apt install -y libglib2.0-dev libsm6 libxrender-dev libxext6

python3.10 -m venv venv
source venv/bin/activate
pip install --upgrade pip

wget https://github.com/nmilosev/pytorch-arm-builds/releases/download/v1.13.1/torch-1.13.1-cp310-cp310-linux_aarch64.whl
pip install torch-1.13.1-cp310-cp310-linux_aarch64.whl
pip install torchvision==0.14.1 torchaudio==0.13.1

pip install transformers==4.38.2 datasets==2.19.0 tokenizers==0.15.2
pip install onnx==1.16.0 onnxruntime==1.18.0 onnxruntime-tools==1.8.3

pip install pandas==2.2.2 numpy==1.26.4 matplotlib==3.8.4 psutil==5.9.8


python -c "import torch; print('Torch:', torch.__version__)"
python -c "import transformers; print('Transformers:', transformers.__version__)"
python -c "import onnx; print('ONNX:', onnx.__version__)"

